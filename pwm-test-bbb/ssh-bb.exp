#!/usr/bin/expect

set USER_ADDR root@192.168.50.3
#set USER_ADDR root@192.168.1.133
set PASSWORD alarm
set COPY_DST /root/

spawn ssh $USER_ADDR
expect {
  -re ".*sword.*" {
    exp_send "$PASSWORD\r"
  }
}
interact

# expect {
#    -re ".*es.*o.*" {
#    exp_send "yes\r"
#    exp_continue
#   }
#   -re ".*sword.*" {
#     exp_send "$PASSWORD\r"
#   }
# }
# interact
